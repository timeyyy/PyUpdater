# command to install dependencies
dependencies:
    pre:
        - sudo apt-get update; sudo apt-get install python-dev build-essential
        - pip install -U pip
        - make deps-dev
        - pip install codecov